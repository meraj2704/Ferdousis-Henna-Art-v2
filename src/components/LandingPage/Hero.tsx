import React from "react";
import { ImageCarousel } from "./ImageCarousel";

const Hero = () => {
  return (
    <div className="w-full h-full">
      <ImageCarousel />
    </div>
  );
};

export default Hero;
